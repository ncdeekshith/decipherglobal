const SUPABASE_URL='https://eiculstbdbbaxactreil.supabase.co/rest/v1';const SUPABASE_ANON_KEY='';const SUPABASE_TABLE='leads';document.querySelector('.menu')?.addEventListener('click',()=>document.querySelector('.nav-links')?.classList.toggle('open'));document.querySelectorAll('.filters').forEach(group=>{const buttons=[...group.querySelectorAll('button')];const cards=[...group.nextElementSibling.querySelectorAll('[data-category]')];buttons[0]?.classList.add('active');buttons.forEach(button=>button.addEventListener('click',()=>{buttons.forEach(b=>b.classList.remove('active'));button.classList.add('active');const filter=button.dataset.filter;cards.forEach(card=>{card.style.display=filter==='All'||card.dataset.category===filter?'block':'none'})}))});document.querySelector('#lead-form')?.addEventListener('submit',async event=>{event.preventDefault();const form=event.currentTarget;const message=form.querySelector('.form-message');const button=form.querySelector('button');const data=Object.fromEntries(new FormData(form).entries());data.source='website';data.created_at=new Date().toISOString();if(!SUPABASE_ANON_KEY){message.textContent='Supabase is ready, but the public anon key is still needed to store this lead.';message.style.color='#b45309';console.log('Lead not sent because SUPABASE_ANON_KEY is missing',data);return}button.disabled=true;button.textContent='Submitting...';try{const response=await fetch(SUPABASE_URL+'/'+SUPABASE_TABLE,{method:'POST',headers:{apikey:SUPABASE_ANON_KEY,Authorization:'Bearer '+SUPABASE_ANON_KEY,'Content-Type':'application/json',Prefer:'return=minimal'},body:JSON.stringify(data)});if(!response.ok)throw new Error(await response.text());message.textContent='Thank you. Your enquiry has been submitted.';message.style.color='#047857';form.reset()}catch(error){message.textContent='Could not submit right now. Please call or email Decipher Global.';message.style.color='#b91c1c';console.error(error)}finally{button.disabled=false;button.textContent='Submit Enquiry'}});